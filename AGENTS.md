# Agent 须知

本仓库是纯静态 HTML 2D、数字图像处理主题的短体验教育游戏（无 Unity、无构建工具）。

1. 先读 `docs/game-design-brief.md`，再读 `.cursor/rules/` 下的项目规则；设计来龙去脉见 `docs/design-journey.md`。
2. 关卡讨论默认走「一条修复流水线」，不要设计互不相关的小游戏合集。全作关卡总数目标约 **15–20 关出头**（第一章已占 7 关）；用小步/大步/组合关拆，不要为凑数开新专题。
3. 游戏本体在 `prototype/chapter1/`（目录名是历史遗留，是游戏本体不是原型）。
4. 设计未锁死。改主线或否决某算法时，回写 `docs/game-design-brief.md`，不要只改聊天记录。
5. `docs/design-journey.md` 维护触发器：用户专门解释某个**设计点**（为什么这样做、取舍理由）时，完成对应工作后主动追加一节记录；实现层面踩坑（修 bug、像素实现问题）不记。不确定算不算设计点时，工作完成后顺带一句询问（带 emoji 醒目）：「📓 这个 XXX 需要收录进设计历程吗？」收录分两级：默认记我认为有设计价值的；用户说「重点收录/重点记」的，在该条目开头标注 **⭐重点**，写文书时优先取材。
